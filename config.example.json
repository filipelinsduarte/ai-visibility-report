{
  "aipeekaboo_api_key": "pk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",

  "_llm_comment": "If running inside Claude Code: set llm_provider to 'claude-cli' and omit llm_api_key entirely. Claude Code will call itself — no separate API key needed.",
  "llm_provider": "claude-cli",

  "_llm_other_providers": "Alternatives: 'anthropic' (sk-ant-...), 'openai' (sk-...), 'gemini' (AIza...), 'groq' (gsk_...), 'openrouter' (sk-or-...), 'mistral'. Add llm_api_key when using these.",

  "_ci_comment": "In GitHub Actions, API keys are read from repository secrets (AIPEEKABOO_API_KEY and LLM_API_KEY) via environment variables. You do not need to set aipeekaboo_api_key or llm_api_key in config.json when running in CI.",

  "brands": [
    {
      "id": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
      "name": "Acme AI",
      "key": "acme",
      "domain": "acme.ai"
    }
  ],
  "report_title": "Acme AI Visibility Report",
  "output_file": "report.html"
}
